# Concert Prep Tool

A GraphQL app that pulls setlist data to help you know what songs to expect
before a show.

## Setup
1. `cp .env.example .env` and fill in real values
2. `docker compose up --build`